### Arrays, Indices and Invariants

- Every index I ever touch in my life has to be valid. In python, a valid index will always follow: 

```python 
    0<=i<len(nums)
```

This is ALWAYS true

-------