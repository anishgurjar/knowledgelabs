### Intro to testing in python using pytest 


- Pytest is a library that helps in writing unit tests.

- Create a file called `test_*` or `*_test`. import the classes / functions you want to test. write a test function, and assert the behaviour in there.
